document.addEventListener('DOMContentLoaded', function () {
    console.log('Page loaded successfully. All assets are ready.');

    // Optional: Add interactive behavior here (e.g., button clicks, animations, dynamic updates)
    // Example: document.getElementById('myButton').addEventListener('click', function() { ... });
});